import React,{  } from "react";

export const MinMaxPricesContext = React.createContext([0,1000]);
export const WishListContext = React.createContext({});
export const SetQueryValueContext = React.createContext(()=>{});
// export default ThemeContext;